//--Directions
//Given a string,return a new with the reversed 
// order of characters 
//--Examples
//reversed ('apple')

function maxChar(str) {
    var charMap = {};
    let max = 0;
    let maxChar = '';

    for (let char of str) {
        if(charMap[char]) {
            
        }
    }
}